info:
  name: testing_create_test_user
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/testing/users
  body:
    type: json
    data: |-
      {
        "username": "testuser",
        "name": "Test User",
        "password": "testpass123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
