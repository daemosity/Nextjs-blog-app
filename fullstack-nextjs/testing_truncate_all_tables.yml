info:
  name: testing_truncate_all_tables
  type: http

http:
  method: DELETE
  url: http://localhost:3000/api/testing/reset
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
